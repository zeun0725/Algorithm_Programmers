# 빗물 트래핑
from typing import List


def up_point(height):
    max_up_index = None
    min_up_index = None

def down_point(height, max_up_index, min_up_index):
    return 0


def solution(height: List[int]) -> int:
    for i, h in enumerate(height[:-1]):
        if height[i] > height[i + 1]:
            pass


    pass


print(solution([0, 1, 0, 2, 1, 0, 1, 3, 2, 1, 2, 1]))
